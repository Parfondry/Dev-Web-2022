const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "neoart.wstr.fr",
      user: "louislombaert",
      password: "Issou_2014",
      database: "Neoart",
    },
    listPerPage: 10,
  };
  module.exports = config;
