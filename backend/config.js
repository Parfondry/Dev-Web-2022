const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "louislombaert",
      password: "Issou_2014",
      database: "neoart",
    },
    listPerPage: 10,
  };
  module.exports = config;